<link rel="stylesheet" href="{{ 'wish.css' | asset_url }}" />
<script src="{{ 'noty.min.js' | asset_url }}" async></script>
<script src="{{ 'wishlist.js' | asset_url }}" defer="defer"></script>
<link rel="stylesheet" href="{{ 'noty.min.css' | asset_url }}" />

<button id="spn_wishlist_btn" data-producthandle={{ product.handle }} data-customerid={{ customer.id }}>Wishlist</button>


{% schema %}
{
  "name": "Wishlist Button",
  "target": "section",
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "product"
    }
  ]
}
{% endschema %}
